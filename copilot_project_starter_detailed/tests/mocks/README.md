# Shared mocks

Use this folder for reusable mocks, fixtures, and fake data providers.

Rules:
- keep data shapes close to real contracts
- name fixtures explicitly
- avoid duplicate ad-hoc mocks in many test files
- separate valid and invalid fixtures when possible
