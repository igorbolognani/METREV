# Supplier Runtime Mapping

This planning note now lives under `contracts/supplier-runtime-mapping.md` so the feature pack follows the maintained internal workflow structure.

Use the contract-scoped note for:

- the planning-only disclaimer
- canonical owner file references
- the detailed runtime and persistence mapping table
- the explicit Phase 3 relational delta
